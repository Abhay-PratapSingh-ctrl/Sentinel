version: '3.8'

services:
  sentinel-bot:
    build: .
    container_name: sentinel-bot
    restart: always
    env_file:
      - ../.env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
