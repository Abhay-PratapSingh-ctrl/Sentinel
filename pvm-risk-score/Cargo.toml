[package]
name = "pvm-risk-score"
version = "0.1.0"
edition = "2021"

[dependencies]
polkavm-derive = "0.13.0"

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
codegen-units = 1
strip = true
